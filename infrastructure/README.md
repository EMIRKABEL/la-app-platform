# Infrastructure

This folder holds **Docker and deployment configuration** for the LA App platform:

- Dockerfiles for each service
- Docker Compose configurations (dev / prod)
- Deployment scripts and CI/CD configuration
- Infrastructure-as-code templates (cloud-agnostic where possible)

## Status

This folder is currently a placeholder. Infrastructure configuration will be added as services are built.
